#!/usr/bin/env python3
"""Entry point for jsonui-test CLI."""

import sys
from pathlib import Path

# Add package to path
sys.path.insert(0, str(Path(__file__).parent))

from jsonui_test_cli.cli import main

if __name__ == "__main__":
    sys.exit(main())
