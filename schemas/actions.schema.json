{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://jsonui.dev/schemas/actions.schema.json",
  "title": "JsonUI Test Actions and Assertions",
  "description": "Definitions for test actions and assertions used in JsonUI test cases",
  "definitions": {
    "elementId": {
      "type": "string",
      "description": "The id attribute of the target element in JsonUI layout",
      "minLength": 1
    },
    "direction": {
      "type": "string",
      "enum": [
        "up",
        "down",
        "left",
        "right"
      ],
      "description": "Direction for scroll/swipe actions"
    },
    "label": {
      "type": "string",
      "description": "Human-readable step name used in logs and reports"
    },
    "stepScreen": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical id of the screen this step runs on (flow tests only). Declared on every step shape because a flow's inlineStep composes this key with an action/assertion through allOf, and each branch closes itself with additionalProperties:false."
    },
    "optional": {
      "type": "boolean",
      "default": false,
      "description": "When true, a failure of this step is recorded as a warning and execution continues"
    },
    "responsive": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "compact",
            "medium",
            "regular",
            "landscape",
            "compact-landscape",
            "medium-landscape",
            "regular-landscape"
          ],
          "description": "Named size-class bucket from the render-side canonical vocabulary; each driver resolves it the way its platform's renderer does"
        },
        {
          "type": "object",
          "properties": {
            "minWidth": {
              "type": "number",
              "minimum": 0,
              "description": "Minimum window width (inclusive). Units: dp (Android) / pt (iOS) / logical px (web)"
            },
            "maxWidth": {
              "type": "number",
              "minimum": 0,
              "description": "Maximum window width (inclusive). Units: dp (Android) / pt (iOS) / logical px (web)"
            },
            "minHeight": {
              "type": "number",
              "minimum": 0,
              "description": "Minimum window height (inclusive). Units: dp (Android) / pt (iOS) / logical px (web)"
            },
            "maxHeight": {
              "type": "number",
              "minimum": 0,
              "description": "Maximum window height (inclusive). Units: dp (Android) / pt (iOS) / logical px (web)"
            },
            "orientation": {
              "type": "string",
              "enum": [
                "portrait",
                "landscape"
              ],
              "description": "Current device/viewport orientation matches"
            }
          },
          "additionalProperties": false,
          "minProperties": 1,
          "description": "Explicit size constraint; keys are ANDed. Uniform numeric evaluation on all platforms (does NOT track iOS size classes — use a named bucket for renderer-faithful gating there)"
        }
      ],
      "description": "Current size matches: a named size-class bucket or an explicit size constraint, resolved at run time on the device"
    },
    "condition": {
      "type": "object",
      "description": "Condition object for 'when' (step pre-condition) and 'repeat.while'. Multiple keys are ANDed.",
      "properties": {
        "visible": {
          "$ref": "#/definitions/elementId",
          "description": "Instant check: element is currently visible"
        },
        "notVisible": {
          "$ref": "#/definitions/elementId",
          "description": "Instant check: element is currently absent or invisible"
        },
        "platform": {
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "ios",
                "android",
                "web",
                "all"
              ]
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "ios",
                  "android",
                  "web"
                ]
              },
              "minItems": 1,
              "uniqueItems": true
            }
          ],
          "description": "Current platform matches"
        },
        "responsive": {
          "$ref": "#/definitions/responsive",
          "description": "Current size class / window size matches (resolved at runtime from the live device/viewport size)"
        },
        "state": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "minLength": 1,
              "description": "Dot-notation path to ViewModel property"
            },
            "equals": {
              "description": "Expected value (any type)"
            }
          },
          "required": [
            "path",
            "equals"
          ],
          "additionalProperties": false,
          "description": "ViewModel state matches (requires a state provider)"
        }
      },
      "additionalProperties": false,
      "minProperties": 1
    },
    "launch": {
      "type": "object",
      "description": "App launch configuration applied before the app under test starts",
      "properties": {
        "clearState": {
          "type": "boolean",
          "default": false,
          "description": "Clear app data before launch (iOS: JSONUI_TEST_CLEAR_STATE env contract; Android: pm clear; Web: cookies + storage)"
        },
        "permissions": {
          "type": "object",
          "description": "Permission grants applied before launch",
          "propertyNames": {
            "enum": [
              "camera",
              "microphone",
              "location",
              "notifications",
              "photos",
              "contacts",
              "calendar",
              "bluetooth"
            ]
          },
          "additionalProperties": {
            "type": "string",
            "enum": [
              "allow",
              "deny",
              "unset"
            ]
          }
        },
        "arguments": {
          "type": "object",
          "description": "Launch arguments passed to the app (iOS: launchEnvironment JSONUI_TEST_ARGS; Android: intent extras; Web: sessionStorage)",
          "additionalProperties": {
            "type": [
              "string",
              "number",
              "boolean"
            ]
          }
        }
      },
      "additionalProperties": false
    },
    "action": {
      "oneOf": [
        {
          "$ref": "#/definitions/tapAction"
        },
        {
          "$ref": "#/definitions/doubleTapAction"
        },
        {
          "$ref": "#/definitions/longPressAction"
        },
        {
          "$ref": "#/definitions/inputAction"
        },
        {
          "$ref": "#/definitions/typeTextAction"
        },
        {
          "$ref": "#/definitions/clearAction"
        },
        {
          "$ref": "#/definitions/scrollAction"
        },
        {
          "$ref": "#/definitions/scrollUntilVisibleAction"
        },
        {
          "$ref": "#/definitions/swipeAction"
        },
        {
          "$ref": "#/definitions/waitForAction"
        },
        {
          "$ref": "#/definitions/waitForAnyAction"
        },
        {
          "$ref": "#/definitions/waitAction"
        },
        {
          "$ref": "#/definitions/backAction"
        },
        {
          "$ref": "#/definitions/hideKeyboardAction"
        },
        {
          "$ref": "#/definitions/screenshotAction"
        },
        {
          "$ref": "#/definitions/alertTapAction"
        },
        {
          "$ref": "#/definitions/selectOptionAction"
        },
        {
          "$ref": "#/definitions/tapItemAction"
        },
        {
          "$ref": "#/definitions/selectTabAction"
        },
        {
          "$ref": "#/definitions/readTextAction"
        },
        {
          "$ref": "#/definitions/repeatAction"
        },
        {
          "$ref": "#/definitions/retryAction"
        },
        {
          "$ref": "#/definitions/setLocationAction"
        },
        {
          "$ref": "#/definitions/addMediaAction"
        },
        {
          "$ref": "#/definitions/setMocksAction"
        },
        {
          "$ref": "#/definitions/setViewportAction"
        },
        {
          "$ref": "#/definitions/setOrientationAction"
        },
        {
          "$ref": "#/definitions/emitHookAction"
        }
      ]
    },
    "tapAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素をタップ",
        "platforms": "`retryTapIfNoChange`（ゴーストタップ緩和）は Web では受理のみ・no-op"
      },
      "properties": {
        "action": {
          "const": "tap"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "text": {
          "type": "string",
          "description": "Specific text portion to tap within the element (e.g., tap '利用規約' within '利用規約に同意する')"
        },
        "retryTapIfNoChange": {
          "type": "boolean",
          "default": false,
          "description": "Re-tap once when the UI did not change after the tap (ghost-tap mitigation; no-op on web)"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "doubleTapAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素をダブルタップ"
      },
      "properties": {
        "action": {
          "const": "doubleTap"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "longPressAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素を長押し"
      },
      "properties": {
        "action": {
          "const": "longPress"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "duration": {
          "type": "integer",
          "minimum": 100,
          "default": 500,
          "description": "Duration in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "inputAction": {
      "type": "object",
      "x-doc": {
        "ja": "指定要素にテキスト入力"
      },
      "properties": {
        "action": {
          "const": "input"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "value": {
          "type": "string",
          "description": "Text to input"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "value"
      ],
      "additionalProperties": false
    },
    "typeTextAction": {
      "type": "object",
      "description": "Type text into the currently-focused field via the keyboard, without targeting an element id. For fields that hold keyboard focus but are not directly targetable (e.g. an invisible code-entry TextField behind visible slots): focus is established app-side (auto-focus or a prior tap on a visible container), then typeText sends the characters.",
      "x-doc": {
        "ja": "フォーカス中の欄へキーボード入力（要素 id 不要。不可視のコード入力欄など向け）"
      },
      "properties": {
        "action": {
          "const": "typeText"
        },
        "value": {
          "type": "string",
          "description": "Text to type into the focused field"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "value"
      ],
      "additionalProperties": false
    },
    "clearAction": {
      "type": "object",
      "x-doc": {
        "ja": "入力内容をクリア"
      },
      "properties": {
        "action": {
          "const": "clear"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "scrollAction": {
      "type": "object",
      "x-doc": {
        "ja": "指定方向へスクロール"
      },
      "properties": {
        "action": {
          "const": "scroll"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "direction": {
          "$ref": "#/definitions/direction"
        },
        "amount": {
          "type": "integer",
          "minimum": 1,
          "description": "Scroll amount in pixels (optional)"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "direction"
      ],
      "additionalProperties": false
    },
    "scrollUntilVisibleAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素が見えるまでスクロール（終端到達で即失敗）"
      },
      "properties": {
        "action": {
          "const": "scrollUntilVisible"
        },
        "id": {
          "$ref": "#/definitions/elementId",
          "description": "Target element to bring into view"
        },
        "container": {
          "$ref": "#/definitions/elementId",
          "description": "Scrollable container to scroll (default: window / first scrollable view)"
        },
        "direction": {
          "$ref": "#/definitions/direction",
          "default": "down",
          "description": "Direction the content moves toward (down reveals content below)"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 20000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "swipeAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素をスワイプ"
      },
      "properties": {
        "action": {
          "const": "swipe"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "direction": {
          "$ref": "#/definitions/direction"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "direction"
      ],
      "additionalProperties": false
    },
    "waitForAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素の出現を待機"
      },
      "properties": {
        "action": {
          "const": "waitFor"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "waitForAnyAction": {
      "type": "object",
      "x-doc": {
        "ja": "いずれかの要素の出現を待機"
      },
      "properties": {
        "action": {
          "const": "waitForAny"
        },
        "ids": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/elementId"
          },
          "minItems": 1,
          "description": "Element ids; the step succeeds when any of them appears"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "ids"
      ],
      "additionalProperties": false
    },
    "waitAction": {
      "type": "object",
      "x-doc": {
        "ja": "指定時間待機"
      },
      "properties": {
        "action": {
          "const": "wait"
        },
        "ms": {
          "type": "integer",
          "minimum": 0,
          "description": "Wait duration in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "ms"
      ],
      "additionalProperties": false
    },
    "backAction": {
      "type": "object",
      "x-doc": {
        "ja": "戻る操作"
      },
      "properties": {
        "action": {
          "const": "back"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action"
      ],
      "additionalProperties": false
    },
    "hideKeyboardAction": {
      "type": "object",
      "description": "Dismiss the soft keyboard if one is visible (no-op when none). Needed when the keyboard covers the tap target and the field type has no return key (e.g. number-pad).",
      "x-doc": {
        "ja": "ソフトキーボードを閉じる（未表示なら no-op）"
      },
      "properties": {
        "action": {
          "const": "hideKeyboard"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action"
      ],
      "additionalProperties": false
    },
    "screenshotAction": {
      "type": "object",
      "x-doc": {
        "ja": "スクリーンショットを保存"
      },
      "properties": {
        "action": {
          "const": "screenshot"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Screenshot file name (without extension)"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "name"
      ],
      "additionalProperties": false
    },
    "alertTapAction": {
      "type": "object",
      "x-doc": {
        "ja": "アラート／ダイアログのボタンをタップ"
      },
      "properties": {
        "action": {
          "const": "alertTap"
        },
        "button": {
          "type": "string",
          "minLength": 1,
          "description": "Button text to tap in the alert dialog (e.g., 'OK', 'Cancel', 'Delete')"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds to wait for alert to appear"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "button"
      ],
      "additionalProperties": false
    },
    "selectOptionAction": {
      "type": "object",
      "x-doc": {
        "ja": "ドロップダウン／ピッカーから選択"
      },
      "properties": {
        "action": {
          "const": "selectOption"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "value": {
          "type": "string",
          "description": "Option value to select"
        },
        "label": {
          "type": "string",
          "description": "Option label (visible text) to select. NOTE: for selectOption, 'label' keeps its original meaning (option text), NOT the universal step-name attribute"
        },
        "index": {
          "type": "integer",
          "minimum": 0,
          "description": "Option index to select (0-based)"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id"
      ],
      "additionalProperties": false
    },
    "tapItemAction": {
      "type": "object",
      "x-doc": {
        "ja": "コレクション内のアイテムをタップ"
      },
      "properties": {
        "action": {
          "const": "tapItem"
        },
        "id": {
          "$ref": "#/definitions/elementId",
          "description": "Collection element id; item is resolved as {id}_item_{index}"
        },
        "index": {
          "type": "integer",
          "minimum": 0,
          "description": "Item index to tap (0-based)"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "index"
      ],
      "additionalProperties": false
    },
    "selectTabAction": {
      "type": "object",
      "x-doc": {
        "ja": "タブを選択"
      },
      "properties": {
        "action": {
          "const": "selectTab"
        },
        "id": {
          "$ref": "#/definitions/elementId",
          "description": "Tab view element id; tab is resolved as {id}_tab_{index}"
        },
        "index": {
          "type": "integer",
          "minimum": 0,
          "description": "Tab index to select (0-based)"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "index"
      ],
      "additionalProperties": false
    },
    "readTextAction": {
      "type": "object",
      "x-doc": {
        "ja": "要素のテキストを実行時変数に読む（`@{変数名}` で後続参照）"
      },
      "properties": {
        "action": {
          "const": "readText"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "variable": {
          "type": "string",
          "minLength": 1,
          "description": "Runtime variable name; later steps reference it as @{name}"
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 5000,
          "description": "Timeout in milliseconds"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "id",
        "variable"
      ],
      "additionalProperties": false
    },
    "repeatAction": {
      "type": "object",
      "x-doc": {
        "ja": "ステップ群を繰り返す（`times`／`while` 併用可、安全上限 100 回）"
      },
      "properties": {
        "action": {
          "const": "repeat"
        },
        "times": {
          "type": "integer",
          "minimum": 1,
          "description": "Iteration count (with 'while': acts as the cap)"
        },
        "while": {
          "$ref": "#/definitions/condition",
          "description": "Loop while this condition holds (checked before each iteration; safety cap 100 when used alone)"
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/step"
          },
          "minItems": 1
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "steps"
      ],
      "anyOf": [
        {
          "required": [
            "times"
          ]
        },
        {
          "required": [
            "while"
          ]
        }
      ],
      "additionalProperties": false
    },
    "retryAction": {
      "type": "object",
      "x-doc": {
        "ja": "失敗時にブロック全体を再実行"
      },
      "properties": {
        "action": {
          "const": "retry"
        },
        "maxRetries": {
          "type": "integer",
          "minimum": 0,
          "maximum": 3,
          "default": 1,
          "description": "Number of retries after the first attempt (capped at 3 by design)"
        },
        "steps": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/step"
          },
          "minItems": 1
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "steps"
      ],
      "additionalProperties": false
    },
    "setLocationAction": {
      "type": "object",
      "x-doc": {
        "ja": "モック位置情報を設定",
        "platforms": "iOS: SDK 依存 ／ Android: best effort ／ Web: setGeolocation"
      },
      "properties": {
        "action": {
          "const": "setLocation"
        },
        "latitude": {
          "type": "number",
          "minimum": -90,
          "maximum": 90
        },
        "longitude": {
          "type": "number",
          "minimum": -180,
          "maximum": 180
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "latitude",
        "longitude"
      ],
      "additionalProperties": false
    },
    "addMediaAction": {
      "type": "object",
      "description": "Seed media fixtures into the device/library so the app can pick them. Android: inserted into the gallery (MediaStore); iOS: seeded into the photo library via PhotoKit — SIMULATOR ONLY (on a real device seeded assets would remain in the user's photo library) and the UITest runner needs a photos-add pre-grant (`xcrun simctl privacy <udid> grant photos-add <uitest-bundle-id>.xctrunner` — the jsonui-test CLI does this automatically); Web: set on a file input. Seeding accumulates across runs on the same simulator/emulator — assert on existence or app state after picking, never on counts. Supported types: png/jpg/jpeg/gif (photo) and mp4 (video)",
      "x-doc": {
        "ja": "メディアフィクスチャを端末に追加（png/jpg/jpeg/gif/mp4。蓄積するため件数でなく存在を検証）",
        "platforms": "Android: ギャラリー（MediaStore）／ iOS: PhotoKit〈シミュレータ限定・photos-add 許可は CLI が自動付与〉／ Web: file input"
      },
      "properties": {
        "action": {
          "const": "addMedia"
        },
        "paths": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1,
          "description": "Media fixture paths. Android: resolved against the on-device media fixtures dir (files must be pushed there beforehand); iOS: resolved inside the UITest bundle, which is flat — use basenames (a subdirectory path that works on Android falls back to its basename on iOS); Web: resolved relative to the test file and set on a file input"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "id": {
          "$ref": "#/definitions/elementId",
          "description": "Web: the file input to target (the input itself, or an element containing one). Omit to use the page's first input[type=file]"
        },
        "timeout": {
          "type": "integer",
          "minimum": 1,
          "description": "Wait for the target element in ms (Web)"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "paths"
      ],
      "additionalProperties": false
    },
    "assertion": {
      "oneOf": [
        {
          "$ref": "#/definitions/visibleAssertion"
        },
        {
          "$ref": "#/definitions/notVisibleAssertion"
        },
        {
          "$ref": "#/definitions/enabledAssertion"
        },
        {
          "$ref": "#/definitions/disabledAssertion"
        },
        {
          "$ref": "#/definitions/textAssertion"
        },
        {
          "$ref": "#/definitions/countAssertion"
        },
        {
          "$ref": "#/definitions/stateAssertion"
        },
        {
          "$ref": "#/definitions/screenshotAssertion"
        },
        {
          "$ref": "#/definitions/openedUrlAssertion"
        },
        {
          "$ref": "#/definitions/screenAssertion"
        }
      ]
    },
    "assertionTimeout": {
      "type": "integer",
      "minimum": 0,
      "description": "Auto-wait timeout in milliseconds (default: runner defaultTimeout, 5000ms). Assertions poll every 100ms until the condition holds."
    },
    "visibleAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "要素が表示されている"
      },
      "properties": {
        "assert": {
          "const": "visible"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id"
      ],
      "additionalProperties": false
    },
    "notVisibleAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "要素が非表示または不存在"
      },
      "properties": {
        "assert": {
          "const": "notVisible"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id"
      ],
      "additionalProperties": false
    },
    "enabledAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "要素が有効"
      },
      "properties": {
        "assert": {
          "const": "enabled"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id"
      ],
      "additionalProperties": false
    },
    "disabledAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "要素が無効"
      },
      "properties": {
        "assert": {
          "const": "disabled"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id"
      ],
      "additionalProperties": false
    },
    "textAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "テキストを検証（`equals`／`contains`）"
      },
      "properties": {
        "assert": {
          "const": "text"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "equals": {
          "type": "string",
          "description": "Exact text match"
        },
        "contains": {
          "type": "string",
          "description": "Partial text match"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id"
      ],
      "oneOf": [
        {
          "required": [
            "equals"
          ]
        },
        {
          "required": [
            "contains"
          ]
        }
      ],
      "additionalProperties": false
    },
    "countAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "要素数を検証（`equals: 0` は不存在で成立）"
      },
      "properties": {
        "assert": {
          "const": "count"
        },
        "id": {
          "$ref": "#/definitions/elementId"
        },
        "equals": {
          "type": "integer",
          "minimum": 0,
          "description": "Expected element count"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "id",
        "equals"
      ],
      "additionalProperties": false
    },
    "stateAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "ViewModel 状態を検証（`path` はドット記法、StateProvider 必須）"
      },
      "properties": {
        "assert": {
          "const": "state"
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Dot-notation path to ViewModel property (e.g., 'user.isLoggedIn')"
        },
        "equals": {
          "description": "Expected value (any type)"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "path",
        "equals"
      ],
      "additionalProperties": false
    },
    "screenshotAssertion": {
      "type": "object",
      "x-doc": {
        "ja": "ベースライン画像とのビジュアル比較（ベースライン無しは新規作成＋警告）"
      },
      "properties": {
        "assert": {
          "const": "screenshot"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Baseline name; compared against <baselineDir>/<platform>/<name>.png. Missing baseline is created and the step passes with a warning."
        },
        "cropId": {
          "$ref": "#/definitions/elementId",
          "description": "Crop the capture to this element's bounding box before comparing"
        },
        "threshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "default": 98.0,
          "description": "Required similarity percentage (100 = pixel-perfect)"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert",
        "name"
      ],
      "additionalProperties": false
    },
    "step": {
      "oneOf": [
        {
          "$ref": "#/definitions/action"
        },
        {
          "$ref": "#/definitions/assertion"
        }
      ]
    },
    "mocksScenarioMap": {
      "type": "object",
      "description": "Map of OpenAPI operationId -> mock scenario name",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    },
    "setMocksAction": {
      "type": "object",
      "x-doc": {
        "ja": "エンドポイント（operationId）ごとのモックシナリオを切り替え"
      },
      "properties": {
        "action": {
          "const": "setMocks"
        },
        "mocks": {
          "$ref": "#/definitions/mocksScenarioMap"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "mocks"
      ],
      "additionalProperties": false
    },
    "setViewportAction": {
      "type": "object",
      "description": "Resize the viewport to sweep responsive breakpoints. Web only (page.setViewportSize); iOS/Android cannot freely resize a running app, so the action is a no-op with a warning there — pair dependent asserts with a matching 'when.responsive' so they skip cleanly on platforms that cannot honor the resize",
      "x-doc": {
        "ja": "ビューポートをリサイズ（レスポンシブ検証）",
        "platforms": "Web のみ。iOS/Android は警告付き no-op（`when.responsive` でゲート）"
      },
      "properties": {
        "action": {
          "const": "setViewport"
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "description": "Viewport width in logical pixels"
        },
        "height": {
          "type": "integer",
          "minimum": 1,
          "description": "Viewport height in logical pixels"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "width",
        "height"
      ],
      "additionalProperties": false
    },
    "setOrientationAction": {
      "type": "object",
      "description": "Rotate to the given orientation (iOS: XCUIDevice; Android: UiDevice; Web: swaps viewport width/height in mobile-emulation contexts, otherwise a no-op with a warning)",
      "x-doc": {
        "ja": "画面の向きを変更",
        "platforms": "iOS: XCUIDevice ／ Android: UiDevice ／ Web: モバイルエミュレーション時のみ（他は警告付き no-op）"
      },
      "properties": {
        "action": {
          "const": "setOrientation"
        },
        "orientation": {
          "type": "string",
          "enum": [
            "portrait",
            "landscape"
          ],
          "description": "Target orientation"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "orientation"
      ],
      "additionalProperties": false
    },
    "emitHookAction": {
      "type": "object",
      "description": "Call a browser-side hook the app registered on window.__jsonuiTestHooks (e.g. an RTDB mock emitter). Web only; iOS/Android no-op with a warning — gate dependent steps with 'when.platform: web'",
      "x-doc": {
        "ja": "アプリが登録したテストフックを呼び出す（`window.__jsonuiTestHooks`）",
        "platforms": "Web のみ。iOS/Android は警告付き no-op（`when.platform` でゲート）"
      },
      "properties": {
        "action": {
          "const": "emitHook"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Hook name registered on window.__jsonuiTestHooks"
        },
        "hookArgs": {
          "type": "array",
          "description": "Positional arguments passed to the hook"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "action",
        "name"
      ],
      "additionalProperties": false
    },
    "openedUrlAssertion": {
      "type": "object",
      "description": "Assert the most recent window.open call recorded by the runner's spy. Web only; gate with 'when.platform: web' in cross-platform tests",
      "x-doc": {
        "ja": "直近の `window.open` 呼び出しの URL を検証",
        "platforms": "Web のみ（`when.platform: web` でゲート）"
      },
      "properties": {
        "assert": {
          "const": "openedUrl"
        },
        "equals": {
          "type": "string",
          "description": "Exact URL to match"
        },
        "contains": {
          "type": "string",
          "description": "URL substring to match"
        },
        "timeout": {
          "$ref": "#/definitions/assertionTimeout"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        }
      },
      "required": [
        "assert"
      ],
      "anyOf": [
        {
          "required": [
            "equals"
          ]
        },
        {
          "required": [
            "contains"
          ]
        }
      ],
      "additionalProperties": false
    },
    "screenAssertion": {
      "type": "object",
      "properties": {
        "assert": {
          "const": "screen"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Canonical id of the screen expected to be displayed. Distinct from the step-level 'screen' key, which says where the step runs: during a transition the two legitimately differ."
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "description": "Max wait in ms for the screen to appear (default: screenTransitionTimeout)"
        },
        "screen": {
          "$ref": "#/definitions/stepScreen"
        },
        "label": {
          "$ref": "#/definitions/label"
        },
        "optional": {
          "$ref": "#/definitions/optional"
        },
        "when": {
          "$ref": "#/definitions/condition"
        }
      },
      "required": [
        "assert",
        "name"
      ],
      "additionalProperties": false,
      "description": "Assert the named screen is displayed. Does NOT assert exclusivity: embedded screens, split panes and tab hosts legitimately show several at once.",
      "x-doc": {
        "ja": "指定画面の表示を検証（排他は主張しない — 埋め込み／タブ等で複数同時あり得る）"
      }
    }
  }
}
