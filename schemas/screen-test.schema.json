{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://jsonui.dev/schemas/screen-test.schema.json",
  "title": "JsonUI Screen Test",
  "description": "Schema for single-screen UI test cases (1:1 mapping with layout JSON)",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to this schema"
    },
    "type": {
      "const": "screen",
      "description": "Test type identifier"
    },
    "source": {
      "type": "object",
      "description": "Source files this test is based on",
      "properties": {
        "layout": {
          "type": "string",
          "description": "Path to the JsonUI layout file (e.g., 'Layouts/Login.json'). Provenance metadata: it is displayed in the generated docs but never opened, so no base directory applies and any spelling is accepted. Contrast 'document', which IS opened and therefore has a defined base."
        },
        "document": {
          "type": "string",
          "description": "Path to the specification document file (e.g., 'docs/screens/login.html' or 'docs/screens/login.md'). Used for linking from Mermaid flow diagrams. Resolved from the doc generator's INPUT DIRECTORY (or its parent) — a different base from 'layout', because this value also becomes the page's location inside the generated site and the URL the diagram links to. Write it as a forward path from the input directory or the project root; a '../'-leading value cannot work, since the page would land outside the output directory."
        }
      },
      "required": [
        "layout"
      ],
      "additionalProperties": false
    },
    "metadata": {
      "type": "object",
      "description": "Test metadata",
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable test name"
        },
        "description": {
          "type": "string",
          "description": "Detailed description of what this test covers"
        },
        "generatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when this test was generated"
        },
        "generatedBy": {
          "type": "string",
          "description": "Agent or tool that generated this test"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tags for filtering tests (e.g., ['smoke', 'regression'])"
        },
        "entry_screen": {
          "type": "boolean",
          "default": false,
          "description": "Mark this screen as an entry point (e.g., splash, login). Entry screens are positioned on the left in Mermaid flow diagrams."
        },
        "group": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "Group name(s) for Mermaid diagram. Can be a string or array. When array, the screen appears in multiple group tabs."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "platform": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ios",
            "android",
            "web",
            "all"
          ]
        },
        {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "ios",
              "android",
              "web"
            ]
          },
          "minItems": 1,
          "uniqueItems": true
        }
      ],
      "default": "all",
      "description": "Target platform(s) for this test"
    },
    "embeddedIn": {
      "type": "string",
      "pattern": "^[A-Z][A-Za-z0-9]*#[a-z][a-zA-Z0-9]*$",
      "description": "Run this screen test inside a parent screen's `Embed` region. Format: 'ParentScreen#regionId' where ParentScreen is the parent screen name (PascalCase) and regionId is the Embed node's `id` attribute in the parent layout (camelCase). When set, the test runner mounts the parent screen first and asserts the screen-under-test renders into the matching Embed slot. Navigation assertions target the parent's NavController/Router (v1 supports `delegate` mode only). See jsonui-cli/docs/plans/2026-05-11-embed-feature.md §11.1."
    },
    "initialState": {
      "type": "object",
      "description": "Initial ViewModel state before test execution",
      "properties": {
        "viewModel": {
          "type": "object",
          "description": "Key-value pairs for ViewModel properties",
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },
    "launch": {
      "$ref": "actions.schema.json#/definitions/launch"
    },
    "mocks": {
      "$ref": "actions.schema.json#/definitions/mocksScenarioMap"
    },
    "setup": {
      "type": "array",
      "description": "Steps to execute before each test case",
      "items": {
        "$ref": "actions.schema.json#/definitions/step"
      }
    },
    "teardown": {
      "type": "array",
      "description": "Steps to execute after each test case",
      "items": {
        "$ref": "actions.schema.json#/definitions/step"
      }
    },
    "cases": {
      "type": "array",
      "description": "Test cases for this screen",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Test case name"
          },
          "description": {
            "type": "string",
            "description": "What this test case verifies"
          },
          "descriptionFile": {
            "type": "string",
            "description": "Path to external description JSON file (relative to test file)"
          },
          "skip": {
            "type": "boolean",
            "default": false,
            "description": "Skip this test case"
          },
          "platform": {
            "oneOf": [
              {
                "type": "string",
                "enum": [
                  "ios",
                  "android",
                  "web",
                  "all"
                ]
              },
              {
                "type": "array",
                "items": {
                  "type": "string",
                  "enum": [
                    "ios",
                    "android",
                    "web"
                  ]
                },
                "minItems": 1,
                "uniqueItems": true
              }
            ],
            "description": "Override platform for this specific case"
          },
          "responsive": {
            "$ref": "actions.schema.json#/definitions/responsive",
            "description": "Run this case only when the current size matches (named size-class bucket or size constraint, resolved at runtime; unmet gates skip the case). ANDs with case-level platform"
          },
          "initialState": {
            "type": "object",
            "description": "Override initial state for this specific case",
            "properties": {
              "viewModel": {
                "type": "object",
                "additionalProperties": true
              }
            },
            "additionalProperties": false
          },
          "args": {
            "type": "object",
            "description": "Default argument values for @{varName} substitution (overridable by flow file-reference args)",
            "additionalProperties": true
          },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "$ref": "actions.schema.json#/definitions/step"
            },
            "description": "Test steps (actions and assertions)"
          }
        },
        "required": [
          "name",
          "steps"
        ],
        "additionalProperties": false
      },
      "minItems": 1
    }
  },
  "required": [
    "type",
    "source",
    "metadata",
    "cases"
  ],
  "additionalProperties": false
}
